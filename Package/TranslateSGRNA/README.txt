Installation guide:

In R, set your working directory to the package root (Or just open TranslateSGRNA as project) and run in terminal:

devtools::install()


Library(TranslateSGRNA) - Will now load in package
?translate_sgrna - will now give info on usage